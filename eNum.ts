//const AISLE=0
//const MIDDLE=1
//const WINDOW=2

enum SeatChoice {
    AISLE = "aisle",
    MIDDLE = "middle",
    WINDOW = "window",
    FOURTH = "fourth" //internation airport // way do declare the seat name
}

// Use a different variable name if needed, or remove the duplicate declaration
const mySeat = SeatChoice.WINDOW