const superHero:string[]=[]
//const heroPower:number[]=[]
const heroPower:Array<number>=[]

type User={
    name:string
    isActive:boolean
}

const allUsers:User[]=[]

const MLModels:number[][]=[
    [255,255,255],
    
]
allUsers.push({name:"thenral",isActive:true})

superHero.push("spiderman")
heroPower.push(2)